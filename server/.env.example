# Database Configuration
DB_HOST=localhost
DB_USER=postgres
DB_PORT=5432
DB_PASSWORD=your_password
DB_NAME=dms_db

# Server Configuration
PORT=3000
HOST=0.0.0.0

# CORS Configuration (Localhost Only)
CLIENT_URL=http://localhost:8005

# JWT Configuration
JWT_SECRET=your_jwt_secret

# Environment
NODE_ENV=development
